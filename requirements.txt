# =============================================================
# requirements.txt — EmotionCine Integration Layer
# Emotion-Aware Multi-Perspective Movie Summarization
# =============================================================
# Install with: pip install -r requirements.txt
# For GPU: install torch separately from https://pytorch.org
# =============================================================

# ── Core UI ──────────────────────────────────────────────────
streamlit>=1.35.0

# ── Deep learning (CPU build; swap for GPU variant if needed)
torch>=2.1.0
torchvision>=0.16.0
torchaudio>=2.1.0

# ── Video analysis (person1_video_module) ─────────────────────
scenedetect[opencv]>=0.6.2
opencv-python>=4.8.0
Pillow>=10.0.0

# ── Audio / emotion analysis (person2_emotion_module) ─────────
librosa>=0.10.0
soundfile>=0.12.0
transformers>=4.38.0
tqdm>=4.65.0

# ── Graph neural network (person3_summary_module) ─────────────
# torch-geometric must be installed separately (see setup.bat)
# torch-geometric>=2.4.0
networkx>=3.1

# ── Visualisation ─────────────────────────────────────────────
plotly>=5.18.0
matplotlib>=3.7.0
seaborn>=0.12.0
kaleido>=0.2.1

# ── Data ──────────────────────────────────────────────────────
numpy>=1.24.0
scipy>=1.10.0
pandas>=2.0.0
scikit-learn>=1.3.0

# ── NLP metrics (evaluation) ──────────────────────────────────
rouge-score>=0.1.2
nltk>=3.8.0

# ── Utilities ─────────────────────────────────────────────────
moviepy>=1.0.3
imageio>=2.31.0
imageio-ffmpeg>=0.4.9
